This repo holds the JSON template for deploying a DCE, 9 custom tables and 9 DCRs.

Simply click the deploy to Azure button below and make sure you are in the correct tenant where this will be deployed.

The resource group this is deployed to should match the workspace resource group.
